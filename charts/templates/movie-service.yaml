apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: movie-service
  template:
    metadata:
      labels:
        app: movie-service
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: movie-service
          image: "{{ .Values.movie.image.repository }}:{{ .Values.movie.image.tag }}"
          imagePullPolicy: {{ .Values.movie.image.pullPolicy }}
          command:
            - sh
            - -c
            - {{ .Values.command.value | quote }}
          ports:
            - containerPort: {{ .Values.movie.service.targetPort }}
          env:
            - name: DATABASE_URI
              value: {{ .Values.movie.env.DATABASE_URI | quote }}
            - name: CAST_SERVICE_HOST_URL
              value: {{ .Values.movie.env.CAST_SERVICE_HOST_URL | quote }}
          livenessProbe:
            httpGet:
              path: {{ .Values.movie.probePath }}
              port: {{ .Values.movie.service.targetPort }}
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: {{ .Values.movie.probePath }}
              port: {{ .Values.movie.service.targetPort }}
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: movie-service
spec:
  type: {{ .Values.movie.service.type }}
  selector:
    app: movie-service
  ports:
    - port: {{ .Values.movie.service.port }}
      targetPort: {{ .Values.movie.service.targetPort }}
      nodePort: {{ .Values.movie.service.nodePort }}
